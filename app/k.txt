
services:
  mysql:
    command: --character-set-results=utf8mb4

services:
  mysql:
    environment:
      - MYSQL_CHARACTER_SET_RESULTS=utf8mb4

cssが適用されていない
ブラウザでstatic_urlにアクセスしたが、更新されていなかった
success: function(response) {
      // リクエストが成功した場合の処理を記述する
      // 例: いいね数の更新など
},
error: function(xhr, status, error) {
  // リクエストが失敗した場合の処理を記述する
}